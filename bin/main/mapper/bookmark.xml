<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="shop.mtcoding.job.model.bookmark.BookmarkRepository">
    <insert id="insert" useGeneratedKeys="true" keyColumn="id" keyProperty="id">
      insert into bookmark_tb(id, user_id, enterprise_id, created_at) values(#{id}, #{userId}, #{enterpriseId}, now()) 
   </insert>
    
   <update id="updateById">
      update bookmark_tb set user_id = #{userId}, recruitment_id = #{userId}, count = #{count} where id = #{id}
   </update>

   <select id="findAll" resultType="shop.mtcoding.job.model.bookmark.Bookmark">
      select * from bookmark_tb
   </select>

   <select id="findById" resultType="shop.mtcoding.job.model.bookmark.Bookmark">
      select * from bookmark_tb where id = #{id}  
   </select>

   <delete id="deleteById">
      delete from bookmark_tb where id = #{id}
   </delete>

   <select id="findByEnterpriseIdAndUserId" resultType="shop.mtcoding.job.model.bookmark.Bookmark">
      select * from bookmark_tb where enterprise_id = #{enterpriseId} and user_id = #{userId}
   </select>
   
   <select id="findByBoardId" resultType="shop.mtcoding.job.model.bookmark.Bookmark">
      select * from bookmark_tb where enterprise_id = #{enterpriseId}
   </select>
</mapper>