<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="shop.mtcoding.job.model.user.UserRepository">
    <insert id="insert">
      insert into user_tb(username, password, name, email, contact, created_at) 
      values(#{username}, #{password}, #{name}, #{email}, #{contact}, now()) 
   </insert>
    
   <update id="updateById">
      update user_tb set username = #{username}, password=#{password}, name = #{name}, email = #{email}, contact = #{contact} where id = #{id}
   </update>

   <select id="findAll" resultType="shop.mtcoding.job.model.user.User">
      select * from user_tb
   </select>

   <select id="findOne" resultType="shop.mtcoding.job.model.user.User">
      select * from user_tb where id = #{id}  
   </select>

   <delete id="deleteById">
      delete from user_tb where id = #{id}
   </delete>
</mapper>